<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Viajes Plus - Busca Hoteles y Vuelos</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      background: #f5f8fa;
      color: #333;
    }
    header {
      background-color: #004aad;
      padding: 20px 40px;
      color: white;
      display: flex;
      align-items: center;
      gap: 15px;
    }
    .logo {
      font-weight: 900;
      font-size: 24px;
      letter-spacing: 2px;
      user-select: none;
      cursor: default;
    }
    main {
      max-width: 900px;
      margin: 40px auto;
      background: white;
      padding: 30px 40px;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(0,0,0,0.1);
    }
    h1 {
      margin-bottom: 25px;
      font-weight: 700;
      color: #004aad;
      text-align: center;
    }
    form {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px 30px;
    }
    label {
      display: block;
      font-weight: 600;
      margin-bottom: 6px;
      color: #555;
    }
    input, select {
      width: 100%;
      padding: 10px 12px;
      border-radius: 6px;
      border: 1.5px solid #ccc;
      font-size: 16px;
      transition: border-color 0.3s ease;
    }
    input:focus, select:focus {
      border-color: #004aad;
      outline: none;
    }
    button {
      grid-column: span 2;
      padding: 14px;
      background-color: #004aad;
      color: white;
      border: none;
      border-radius: 8px;
      font-weight: 700;
      font-size: 18px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }
    button:hover {
      background-color: #00337a;
    }
    footer {
      margin-top: 50px;
      text-align: center;
      font-size: 14px;
      color: #888;
    }
    @media (max-width: 600px) {
      form {
        grid-template-columns: 1fr;
      }
      button {
        grid-column: span 1;
      }
    }
  </style>
</head>
<body>
  <header>
    <div class="logo">ViajesPlus</div>
  </header>
  <main>
    <h1>Busca Hoteles y Vuelos</h1>
    <form id="searchForm">
      <div>
        <label for="type">Tipo de búsqueda</label>
        <select id="type" name="type" required>
          <option value="" disabled selected>Selecciona</option>
          <option value="vuelos">Vuelos</option>
          <option value="hoteles">Hoteles</option>
          <
